import { describe, it, expect } from 'vitest';
import { bigJump } from './big-jump';

describe('Big Jump', () => {
  it('should work for basic test case', () => {
    // TODO: Add test cases
    expect(true).toBe(true);
  });
});
